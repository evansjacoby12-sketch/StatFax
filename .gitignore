node_modules/
server/.build/
model-lab/data/
dist/*
# Keep the rolling reconcile/backtest log committed — it seeds calibration +
# the Results view on a fresh clone / first cloud deploy. Everything else in
# dist/ (daily.json, zone-cache, …) is regenerated by `npm run slate`.
!dist/backtest-log.json
*.out.json
.env
.omc/
